## What’s Changed

Maintenance release to refresh the template toolchain after `v0.1.0` and keep the
generated repo defaults current.

### Dependency And Tooling Updates

- Upgraded the root workspace toolchain to current compatible versions, including:
  - `pnpm@10.32.0`
  - `eslint@10.0.3`
  - `@eslint/js@10.0.1`
  - `typescript-eslint@8.57.0`
  - `lefthook@2.1.3`
  - `npm-check-updates@19.6.3`
- Upgraded generated package defaults, including:
  - `tsdown@0.21.1`
  - `publint@0.3.18`
  - `@types/node@24.12.0`
- Kept the template on the Node 24 type line intentionally, rather than moving to
  `@types/node@25`, so the generated repo still matches its Node 24 baseline.

### CI And Workflow Updates

- Refreshed all GitHub Actions workflow references to the latest release tags in use by
  this repo.
- Fixed the admin `template-render-smoke` job by installing `uv` with
  `astral-sh/setup-uv`, so the flowmark-backed markdown hook works in GitHub Actions.

### Validation

- Passed `pnpm compile-template`
- Passed `pnpm check`
- Passed `python admin/smoke_test_template.py`
- Passed GitHub Actions CI on `main` after the upgrade commit
